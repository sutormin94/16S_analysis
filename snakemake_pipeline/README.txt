# The pipeline for analysing 16S RNA with snakemake

# Use the following command to start the papeline:
	
  $ snakemake --cores 40

# Set a path for output directory:
   
  If files locate in /home/.../parent_directory/sample_directory/files.fastq.gz, print a path to sample_directory without a last slash:
  
  /home/.../parent_directory
  
# Run pipeline

# The results of pipeline will store in /home/.../parent_directory/results/
